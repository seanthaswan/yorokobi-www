---
export interface MiniBlockData {
  iconUrl?: string;
  title?: string;
  text?: string;
  linkUrl: string;
}

const { blockData } = Astro.props;
---

<section class="flex mb-8 min-h-50 border shadow shadow-skinShadow/40 hover:shadow-inner rounded-sm pointer">
  <a href={blockData.linkUrl} class="flex flex-1 button opacity-80 font-bold px-5 rounded-sm text-center items-center justify-evenly">
    <div class="flex flex-1 gap-6 items-center">
      {blockData.title && <span class="font-bold">{blockData.title}</span>}
    </div>
    <div class="span">→</div>
  </a>
</section>
